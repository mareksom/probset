1. > pip install -r requirements.txt

2. From external site http://code.google.com/p/postmarkup/downloads/list, download postmarkup-1.2.0.tar.gz and install:
> tar xzf postmarkup-1.2.0.tar.gz
> cd postmarkup-1.2.0
> python3 setup.py install

3. Setup django project:
> ./manage.py syncdb
In this step you should create a superuser.

4. Run temporary server:
> ./manage.py runserver
